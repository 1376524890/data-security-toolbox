services:
  postgres:
    restart: always
  redis:
    restart: always
  backend:
    restart: always
  worker:
    restart: always
  frontend:
    ports:
      - "${HTTP_PORT:-80}:80"
    restart: always
  flower:
    restart: always
