# Build context only needs sources + lockfile. node_modules/dist are rebuilt
# inside the image, and the host's platform-specific binaries must not be copied.
node_modules
dist
.vite
.env*
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.local
.DS_Store
